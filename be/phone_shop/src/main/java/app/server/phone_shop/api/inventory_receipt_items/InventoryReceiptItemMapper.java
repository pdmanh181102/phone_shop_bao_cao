// package app.server.phone_shop.api.inventory_receipt_items;

// import org.mapstruct.Mapper;
// import org.mapstruct.Mapping;

// @Mapper(componentModel = "spring")
// public interface InventoryReceiptItemMapper {
// @Mapping(target = "receiptUid", source = "receipt.uid")
// @Mapping(target = "productUid", source = "entryItem.product.uid")
// @Mapping(target = "entryItemUid", source = "entryItem.uid")
// @Mapping(target = "quantity1", source = "entryItem.quantity")
// InventoryReceiptItemDto toDto(InventoryReceiptItemEntity entity);
// }
